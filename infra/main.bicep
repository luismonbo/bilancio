// Bilancio — top-level Bicep deployment
// Deploys all resources to italynorth via module composition.
// Run: az deployment group create --resource-group rg-bilancio-prod --template-file infra/main.bicep --parameters infra/parameters.prod.bicepparam

// TODO: implement modules and wire them together
